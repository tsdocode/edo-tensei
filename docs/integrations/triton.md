# Triton integration

The Triton Python-backend model repository lives in [`examples/06_triton_snapshot`](../../examples/06_triton_snapshot/). The current command validates server startup and inference; native Docker mount-namespace restore remains experimental.
