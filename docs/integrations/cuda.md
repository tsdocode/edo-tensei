# CUDA integration

Native CUDA fixtures live in [`examples/05_cuda_restore`](../../examples/05_cuda_restore/). The combined fixture validates the CUDA-before-CRIU order; the model-sized fixture validates a deterministic GPU allocation checksum.
