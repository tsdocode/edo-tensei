# PyTorch integration

The runnable model warm-start path is [`examples/03_pytorch_warm_start`](../../examples/03_pytorch_warm_start/). It loads a Hugging Face model, warms inference, checkpoints once, and verifies the initialization marker and GPU checksum after restore.
