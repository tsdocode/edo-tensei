# Kubernetes integration

The same-node GPU restore walkthrough is [`examples/07_kubernetes_migration`](../../examples/07_kubernetes_migration/), backed by the manifests under [`kubernetes/`](../../kubernetes/). Controller-managed discovery and cross-node storage remain future work.
