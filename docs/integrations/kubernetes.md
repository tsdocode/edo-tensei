# Kubernetes integration

The same-node GPU restore walkthrough is [`examples/08_kubernetes_migration`](../../examples/08_kubernetes_migration/). It includes the DaemonSet agent, RBAC, source/placeholder Pods, CRD sketch, CUDA workload fixture, and vLLM manifests. Controller-managed discovery and cross-node storage remain future work.
