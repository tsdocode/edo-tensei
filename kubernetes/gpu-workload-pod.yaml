apiVersion: v1
kind: Pod
metadata:
  name: edo-gpu-workload
  namespace: edo-system
  labels:
    app: edo-gpu-workload
spec:
  restartPolicy: Never
  runtimeClassName: nvidia
  containers:
    - name: workload
      image: edo-gpu-workload:test
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: edo-binaries
          mountPath: /opt/edo/bin
          readOnly: true
        - name: edo-libraries
          mountPath: /opt/edo/lib
          readOnly: true
        - name: snapshots
          mountPath: /var/lib/edo-snapshots
      resources:
        limits:
          nvidia.com/gpu: 1
  volumes:
    - name: edo-binaries
      hostPath:
        path: /opt/edo/bin
        type: Directory
    - name: snapshots
      hostPath:
        path: /var/lib/edo-snapshots
        type: DirectoryOrCreate
    - name: edo-libraries
      hostPath:
        path: /opt/edo/lib
        type: Directory
